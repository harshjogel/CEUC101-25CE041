#include <stdio.h>
int main()
{
    int age;
    printf("enter your age");
    scanf("%d",&age);
    if (age>=12 && age<=60)

        printf("entry with fee=50");

    else
        printf("free entry :)");
}
